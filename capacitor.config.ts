import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.rishavpunatar.sursaath',
  appName: 'SurSaath',
  webDir: 'dist',
  android: {
    backgroundColor: '#12151c',
  },
}

export default config
